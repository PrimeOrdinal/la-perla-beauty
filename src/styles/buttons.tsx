export const buttons = `
    button {
        appearance: unset;
        background: unset;
        border-radius: 12px;
        border: unset;
        cursor: pointer;
        font-family: 'Quicksand';
        letter-spacing: 1px;
        padding: 1rem 4rem;
        text-align: center;
        text-transform: uppercase;

        &:disabled {
            cursor: not-allowed;
        }
    }
`
